package
{
	import flash.display.Sprite;
	
	/**
	 * Class does nothing but is needed by FlashBuilder IDE.
	 * FlashBuilder does allow UnitTest to co-exist in a Library SWC
	 * project, thus this project is designed to help authoring of
	 * Unit Tests.  Use the Maven script to compile the latest swc binary.
	 */
	public class Main extends Sprite {
		public function Main() {
		}
	}
}